apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: senselab-core-prod

resources:
  - ../../base

namePrefix: prod-

patches:
  - patch: |-
      - op: replace
        path: /spec/replicas
        value: 3
    target:
      kind: Deployment
      name: senselab-core-api
  - patch: |-
      - op: replace
        path: /spec/replicas
        value: 3
    target:
      kind: Deployment
      name: senselab-core-queue-worker
  - patch: |-
      - op: replace
        path: /spec/minReplicas
        value: 3
      - op: replace
        path: /spec/maxReplicas
        value: 20
    target:
      kind: HorizontalPodAutoscaler
      name: senselab-core-api-hpa
  - patch: |-
      - op: replace
        path: /spec/resources/requests/storage
        value: 50Gi
    target:
      kind: PersistentVolumeClaim
      name: mysql-pvc

configMapGenerator:
  - name: senselab-core-config
    behavior: merge
    literals:
      - APP_ENV=production
      - APP_DEBUG=false
      - APP_URL=https://api.senselab.com
      - LOG_LEVEL=warning
